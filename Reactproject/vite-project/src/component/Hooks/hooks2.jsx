import { useEffect,useState } from "react";

export default function Hooks(){

  const[count, setCount ] = useState(0);
  useEffect(()=> {
    setTimeout(() => {
      
    }
  })
  return(
    <>
    
    </>
  )
}